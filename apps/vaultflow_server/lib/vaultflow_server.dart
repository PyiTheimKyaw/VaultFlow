/// Server-side services for VaultFlow (auth, sync, storage, transfers).
///
/// Route handlers under `routes/` stay thin and delegate to code in this
/// library so it can be unit-tested without the HTTP layer.
library;
