name: vf_domain
description: "Domain entities, repository interfaces and use cases (pure Dart)."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.1.0
  meta: ^1.16.0
  vf_core:
    path: ../vf_core
  vf_protocol:
    path: ../vf_protocol

dev_dependencies:
  build_runner: ^2.16.1
  freezed: ^4.0.1
  test: ^1.25.0
