import 'package:flutter_test/flutter_test.dart';
import 'package:vf_transfer/vf_transfer.dart';

void main() {
  test('package is wired into the workspace', () {
    expect(vfTransferPackageName, 'vf_transfer');
  });
}
