name: vf_transfer
description: "Resumable chunked upload/download engine."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  vf_core:
    path: ../vf_core
  vf_database:
    path: ../vf_database
  vf_domain:
    path: ../vf_domain
  vf_network:
    path: ../vf_network
  vf_protocol:
    path: ../vf_protocol

dev_dependencies:
  flutter_test:
    sdk: flutter
