name: vf_transfer
description: "Resumable chunked uploads and ranged downloads: chunk planner, workers, hasher, engine."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  crypto: ^3.0.7
  dio: ^5.11.1
  drift: ^2.34.4
  flutter:
    sdk: flutter
  meta: ^1.16.0
  path: ^1.9.1
  vf_core:
    path: ../vf_core
  vf_database:
    path: ../vf_database
  vf_domain:
    path: ../vf_domain
  vf_network:
    path: ../vf_network
  vf_protocol:
    path: ../vf_protocol

dev_dependencies:
  flutter_test:
    sdk: flutter
  vaultflow_server:
    path: ../../apps/vaultflow_server
  vf_sync:
    path: ../vf_sync
