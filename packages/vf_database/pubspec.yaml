name: vf_database
description: "Drift local database: schema, DAOs, migrations, encrypted openers."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  drift: ^2.34.4
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.6
  sqlite3: ^3.5.2
  vf_core:
    path: ../vf_core
  vf_domain:
    path: ../vf_domain
  vf_protocol:
    path: ../vf_protocol

dev_dependencies:
  build_runner: ^2.16.1
  drift_dev: ^2.34.6
  flutter_test:
    sdk: flutter
