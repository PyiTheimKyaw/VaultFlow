name: vf_database
description: "Drift local database: schema, DAOs, migrations, encrypted openers."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  vf_core:
    path: ../vf_core
  vf_domain:
    path: ../vf_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
