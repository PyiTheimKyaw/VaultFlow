name: vf_network
description: "HTTP client: Dio factory, auth interceptor, connectivity monitor, typed API client."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  connectivity_plus: ^7.3.1
  dio: ^5.11.1
  flutter:
    sdk: flutter
  vf_core:
    path: ../vf_core
  vf_protocol:
    path: ../vf_protocol

dev_dependencies:
  flutter_test:
    sdk: flutter
