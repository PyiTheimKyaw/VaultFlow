name: vf_sync
description: "Sync engine: outbox processor, change puller, conflict detection and resolution, scheduler."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0
  vf_core:
    path: ../vf_core
  vf_database:
    path: ../vf_database
  vf_domain:
    path: ../vf_domain
  vf_network:
    path: ../vf_network
  vf_protocol:
    path: ../vf_protocol

dev_dependencies:
  dio: ^5.11.1
  fake_async: ^1.3.3
  flutter_test:
    sdk: flutter
  vaultflow_server:
    path: ../../apps/vaultflow_server
