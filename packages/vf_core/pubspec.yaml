name: vf_core
description: "Result/Failure types, ids, clock, logger — zero Flutter dependencies."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
