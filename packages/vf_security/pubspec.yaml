name: vf_security
description: "Secure storage, biometric gate, app lock controller, PIN vault."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  vf_core:
    path: ../vf_core

dev_dependencies:
  flutter_test:
    sdk: flutter
